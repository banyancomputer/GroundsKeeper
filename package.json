{
	"name": "GroundsKeeper",
	"version": "1.0.0",
	"description": "Backend for Tombolo",
	"main": "index.js",
	"repository": "git@github.com:banyancomputer/GroundsKeeper.git",
	"author": "alex@banyan.computer",
	"license": "MIT",
	"private": true,
	"workspaces": [
		"./minibus",
		"./functions"
	],
	"scripts": {
		"format": "prettier --write .",
		"prepare": "husky install",
		"build": "yarn workspaces run build",
		"test": "yarn workspaces run build && firebase emulators:exec 'yarn workspaces functions test && yarn workspaces minibus test'"
	},
	"devDependencies": {
		"@commitlint/cli": "^17.6.1",
		"@commitlint/config-conventional": "^17.6.1",
		"husky": "^8.0.3",
		"prettier": "^2.8.8"
	},
	"engines": {
		"npm": ">=7.0.0"
	},
	"dependencies": {
		"firebase-tools": "^11.29.1"
	}
}
