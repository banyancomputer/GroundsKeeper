rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // allow read, write: if false;
      // Allow read and writes by a Service account
      allow read, write: if request.auth.uid != null;
    }
  }
}